# ActionWise Data Science — dependencies
# NB: this project is pinned to Python 3.11, not the system 3.14.
# shap depends on numba/llvmlite, which lag new CPython releases by months.

# Core data stack
pandas>=2.0
numpy>=1.26,<2.1
pyarrow>=15.0
scipy>=1.11

# Survey data (SPSS .sav)
pyreadstat>=1.2

# Storage / shared cache
duckdb>=1.0

# Modelling
scikit-learn>=1.3
statsmodels>=0.14        # OrderedModel — ordered probit for RHI
lightgbm>=4.0            # PGI drivers
shap>=0.44               # driver attribution
girth>=0.8               # 2PL IRT (marginal ML); scipy fallback in models/irt.py

# Dashboard
streamlit>=1.30
plotly>=5.0

# Notebooks
jupyter>=1.0
matplotlib>=3.8

# Docs — markdown tables in the generated model cards
tabulate>=0.9

# Tests
pytest>=8.0
